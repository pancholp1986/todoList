const config = {
    urlLoadItems: 'https://jsonplaceholder.typicode.com/todos',
    userLogin: {
        username: 'admin',
        password: 'admin',
        id: 1
    },
    pagination: {
        from: 1, // from
        to: 5 // items per page
    }
}

export default config;